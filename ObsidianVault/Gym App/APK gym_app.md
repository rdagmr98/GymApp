# APK gym_app
`flutter build apk --split-per-abi --release` → 3 file: `gym_app-X.Y.Z+N-arm64-v8a.apk` / `-armeabi-v7a.apk` / `-x86_64.apk`
Più `flutter build appbundle --release` → `.aab` per Play Store. Output in `Documents\releases\gym_app\`.
← [[gym_app]] → [[releases]]
