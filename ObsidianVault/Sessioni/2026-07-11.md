---
tags: [sessione, caae, materiale-didattico, bugfix, python]
---
# Sessione 2026-07-11

## Materiale Didattico CAAE — chiusura bug quiz non conforme (91/91 domande) + risolta decisione Modulo 3

Hub: [[CAAE/Materiale Didattico]]

Continuazione diretta della sessione precedente ([[Sessioni/2026-07-10]]): completato il fix sistematico del formato quiz avviato ieri, e risolta la decisione pendente sul gap del Modulo 3 di b2.

### Bug quiz non conforme — completato il fix su 51.1, 53.1, 53.2

Ieri era stato scoperto (script `check_quiz_format.py`, scratchpad) che 7/23 sottomoduli avevano domande quiz in formato stringa aperta invece della tupla obbligatoria `(domanda, [3 alternative], indice_corretto 0-2)` richiesta dal formato d'esame AER(EP).P-66 — regola ribadita duramente dall'utente (verbatim): *"continua e ricorda che le domande devono essere come vuole la p66 3 alternative ecc, ricordalo!"*. Ieri erano già stati corretti 1.1/1.2/1.3 (`mod01.py`) e 50.1 (`mod50.py`). Oggi completati i restanti:

- **`content_51_1`** (`mod51.py`, Sistema 94 — armamento) — 16 domande riscritte: Master Arm, bus digitale MIL-STD-1553 vs analogico, forza di espulsione (F=P×A, 10 MPa × 5 cm² = 5.000 N), velocità di espulsione (impulso-quantità di moto, 2,5 m/s), MIL-STD-1760, forza di rinculo (conservazione quantità di moto, 5.000 N media), boresight/harmonization, distanza laser da tempo di volo (40 µs → 6 km), HERO/EED, hang-up/misfire.
- **`content_53_1`** (`mod53.py`, Sistema 93 — sorveglianza) — 16 domande: sensori attivi/passivi, distanza radar da tempo di volo (200 µs → 30 km), legge 1/R⁴, Doppler/MTI, Stefan-Boltzmann/Wien per IR, finestre atmosferiche IR, MAD (caduta di campo 1/r³), sonar vs radar (velocità del suono in acqua ~1500 m/s), distanza sonar (1,5 s → ~1.125 m), fusione multi-sensore, clutter vs bersaglio reale.
- **`content_53_2`** (`mod53.py`, Sistema 97 — registrazione immagini) — 16 domande: catena ottica scena→immagine, equazione delle lenti sottili 1/f=1/p+1/q, f-number e legge 1/N², effetto fotoelettrico e saturazione full-well, CCD vs CMOS, rumore fotonico √N, matrice di Bayer, teorema di Nyquist/aliasing, GSD=p×H/f, Rayleigh 1/λ⁴, compressione video intra/inter-frame.

`content_53_3` era già conforme, non toccato (verificato riga per riga prima di editare, dato che `mod53.py` usa un pattern diverso — variabile locale `questions = [...]` referenziata dopo, non `"questions": [...]` inline come negli altri file — servita cross-verifica fra posizione di `def content_53_N` e blocco `questions` per non confondere i tre sottomoduli).

**Verifica finale**: rieseguito `check_quiz_format.py` su tutto `build.CONTENT` (23 sottomoduli) → **0/23 non conformi** (da 7/23 a inizio lavoro, ieri). Bug chiuso: 91/91 domande corrette. Regola e cronologia completa in memoria persistente `feedback_quiz_3_alternatives`.

### Decisione Modulo 3 (Electrical Fundamentals) — risolta, nessuna domanda pendente

Il gap era: `reference.json` (app "corsi") copre solo 3.1-3.12 (61h), il programma ufficiale ha 18 sottomoduli fino a 3.18 (90h teoria + 20h* pratica) — mancavano 3.13-3.18 (29h): AC Theory, R/C/L Circuits, Transformers, Filters, AC Generators, AC Motors. Ieri la decisione era stata lasciata aperta ("da chiedere all'utente"). Trovata la conferma nel syllabus combinato `Documents\BTC\btc\02 - SYLLABUS_EI_B1_Combinato_(T+P)_Rev.1 (2).pdf`, che riporta 3.13-3.18 come contenuto ufficiale reale del Modulo 3 (90h+20h* totali) — non un'invenzione, solo un gap di `reference.json`. Decisione: **si scrive il Modulo 3 per intero, 3.1-3.18**. Aggiornate le note vault ([[CAAE/Materiale Didattico]] e [[CAAE/Programmi Ufficiali — Argomenti per Sottomodulo]]) che riportavano ancora la vecchia formulazione "decisione aperta". `Documents\BTC\btc\` registrato come seconda fonte di ground-truth (memoria `project_materiale_didattico_caae`), pari livello a `Documents\programmi\`.

### Modulo 2 (Physics) — verificata divergenza TB1/TB2, non un gap

Prima di scrivere contenuto, verificato un disallineamento fra i dati di `reference.json` per il Modulo 2 (TB1: 90h totali; TB2: 80h) e la tabella già presente in vault (sourced solo da TB2). `b1.pdf` è un PDF scansionato (0 caratteri estraibili, PyMuPDF) — nessun dump testuale disponibile; lette direttamente le pagine rasterizzate `Documents\programmi\b1_pages\page_12/13/14.png`. Risultato: **TB1 e TB2 hanno davvero programmi diversi per il Modulo 2**, non un errore. Divergono su 4 dei 6 argomenti dentro l'ombrello 2.2 Mechanics (Statics, Kinetics, Dynamics-Mass, Fluid dynamics-viscosità): TB1 li richiede a livello 2/10h ciascuno, TB2 a livello 1 con ore ridotte (8h/8h/6h/8h) — gli altri 7 sottomoduli sono identici. Tabella TB1 completa aggiunta in [[CAAE/Programmi Ufficiali — Argomenti per Sottomodulo]] accanto a quella TB2 già presente. Scoperto anche che `reference.json` per TB1 è esatto al dettaglio (bucketing per codice combacia col PDF), mentre per TB2 il bucketing per codice è sbagliato pur avendo il totale giusto per coincidenza (80h=80h) — da correggere in copertina con override mirato in `build.py`, non in reference.json.

**Decisione**: si scrive un solo `content_2_X` per codice (stesso pattern Modulo 1: 2.1, 2.2, 2.3, 2.4, 2.5), 2.2 alla profondità massima (livello 2 ovunque, pattern già usato per 53.x/54.1), copertina mostra livello/ore del corso specifico da reference.json.

### Modulo 2 — content_2_2 (Mechanics): scritte 2.2.1-2.2.3(a) nelle sessioni precedenti, oggi 2.2.3(b)

Trovati e corretti 8 caratteri font-unsafe pre-esistenti in `mod02.py` (sezione 2.1 Matter, da una sessione precedente, non miei): 6× freccia "→" (U+2192) in un elenco puntato sui cambiamenti di stato → sostituita con "->" ASCII (pattern già in uso in `mod01.py`/`mod50.py`, verificato via grep); σ/ε in una didascalia di formula (stress/strain) → sostituiti con "lettera greca sigma"/"lettera greca epsilon" (pattern già in uso altrove nello stesso file, es. riga 532 "lettera greca omega"). Verificato pulito con `python qa.py --src`.

Durante il controllo scoperti altri **79 caratteri font-unsafe** in moduli già "completati" in sessioni precedenti: `mod01.py` (60, prevalentemente − ÷ ≠ π θ √ ≈ e pedici unicode ₀₁₂), `mod50.py` (2, λ), `mod51.py` (1, Δ), `mod53.py` (16, → λ ⁴ ∝). Probabile causa: Gate A di `qa.py` (scansione caratteri sorgente) aggiunto/esteso dopo il QA "Fase 1" di quei moduli, che verificava solo Gate B (typo/replacement character sui PDF renderizzati). Non corretti ora (fuori scope, richiedono riformulazione contestuale non meccanica) — tracciati in task #55.

Scritta sezione **2.2.3(b) "Dinamica: quantità di moto, urti, principi giroscopici e attrito"** in `mod02.py`: quantità di moto e teorema dell'impulso (derivato da F=ma), conservazione della quantità di moto negli urti (con esempio numerico carrelli 3kg+1kg e rimando all'esempio di rinculo già in 51.1), momento angolare e rigidità giroscopica nello spazio (analogia diretta con la quantità di moto), precessione giroscopica (con nota su piatto oscillante/swashplate elicottero), attrito statico/dinamico (con nota su sistemi antiskid), resistenza al rotolamento. 3 figure usate (Fig. 2.2-8/9/10: `2_2_momentum.png`, `2_2_gyroscopic.png`, `2_2_friction.png`, già pronte in `figures.py`). File verificato font-safe (`qa.py --src` silenzioso su mod02.py) e sintatticamente coerente (unica parentesi aperta rimasta è quella esterna "sections", attesa finché il modulo non è chiuso).

Mancano ancora in content_2_2: 2.2.4(a) Density, 2.2.4(b) Viscosity/Bernoulli, poi summary/glossary/questions/references di chiusura funzione.

### Modulo 2 — content_2_2 completato: 2.2.4(a)/(b) + chiusura, bug LaTeX trovato e corretto

Completate le ultime due sezioni: **2.2.4(a)** "Densità, peso specifico e densità relativa dei fluidi" (densità/peso specifico/densità relativa, densimetro — Fig. 2.2-11) e **2.2.4(b)** "Viscosità, resistenza dei fluidi ed equazione di Bernoulli" (moto laminare, streamlining/distacco strato limite, equazione di continuità, equazione di Bernoulli, effetto Venturi, tubo di Pitot — Fig. 2.2-12/13). Poi scritta la chiusura della funzione: **summary** (6 paragrafi, uno per sotto-argomento), **glossary** (52 termini: 9+9+9+9+5+11), **questions** (24 domande, 4 per sotto-argomento, tutte tuple 3-alternative conformi P-66), **references** (4 citazioni: AER(EP).P-66, programma addestrativo CAAE, EASA Part-66/AMC Modulo 2, manuali tecnici del costruttore).

`content_2_2` è ora **completo**: 6 sezioni (2.2.1 Statica+idrostatica, 2.2.2 Cinetica, 2.2.3a Dinamica-lavoro/energia, 2.2.3b Dinamica-quantità di moto/giroscopia/attrito, 2.2.4a Densità, 2.2.4b Viscosità/Bernoulli), **13 figure** (Fig. 2.2-1..13), 52 termini di glossario, 24 domande, 4 riferimenti.

**Bug scoperto e corretto — comandi LaTeX `\big(`/`\big)` non supportati da matplotlib mathtext**: durante la verifica (prima esecuzione **live** mai fatta di `content_2_2()` — fino ad ora solo controllata staticamente) l'import+chiamata della funzione ha sollevato `ParseFatalException: Unknown symbol: \big` nella formula del baricentro in 2.2.1 (`W × x_CG = Σᵢ(mᵢ·g·xᵢ)`, scritta con `\big(...\big)` per il dimensionamento automatico delle parentesi — sintassi LaTeX completa non supportata dal sottoinsieme mathtext di matplotlib). Bug preesistente da una sessione precedente, invisibile sia a `qa.py --src` (controlla solo i caratteri sorgente) sia a `ast.parse` (controlla solo la sintassi Python) — nessuno dei due renderizza davvero le formule; l'unico modo per scoprirlo era eseguire per davvero la funzione forzando il render di ogni `figures.formula_png()`. **Fix**: rimossi `\big(`/`\big)`, sostituiti con parentesi semplici `(`/`)` (sufficienti, il contenuto non è particolarmente alto). **Lezione per il resto del Modulo 2/3**: dopo aver scritto ogni sottomodulo, eseguire anche un test di import+chiamata della funzione (non solo `qa.py --src`/`ast.parse`) per intercettare errori di rendering LaTeX prima del build completo con `build.py`.

**Verifica finale**: `qa.py --src` pulito (0 nuove violazioni, resta il baseline noto di 79 caratteri in mod01/50/51/53, task #55); `ast.parse` OK; esecuzione live di `content_2_1()` e `content_2_2()` entrambe senza eccezioni; struttura verificata a programma (chiavi nell'ordine corretto, 6 sezioni, 6 paragrafi di summary, 52 termini di glossario, 24 domande 100% conformi al formato tupla 3-alternative, 4 riferimenti).

Task #50 marcato completed.

Confermato (per corroborazione, non nuova richiesta): `Materiale Didattico/` resta **locale, non pushata** — nessuna regola `.gitignore` la esclude, è semplicemente non tracciata, coerente con la decisione esplicita del 2026-07-10 ("Non pushare, resta locale"). Nessuna azione git su `mod02.py` in questa sessione.

### Prossimo
content_2_2 (Mechanics) completo e verificato a livello di struttura/sorgente. Prossimo: **content_2_3** (Thermodynamics), poi **content_2_4** (Optics), **content_2_5** (Wave Motion and Sound), poi build+QA Modulo 2 completo (task #54: `build.py` su tutti i corsi + `qa.py` + spot-check visivo — copre anche 2.1/2.2, mai ancora renderizzati in PDF), poi Modulo 3 completo (3.1-3.18). Applicare fin da subito il formato quiz corretto (3 alternative), il controllo licenza foto reali già in memoria (`feedback_image_license_check`), e il nuovo test di import+chiamata per le formule LaTeX. Task #55 (79 caratteri font-unsafe residui) resta aperto, da riprendere quando non interrompe il flusso principale.

---
← [[Sessioni/2026-07-10]]
