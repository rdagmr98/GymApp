# Excel per Worker
Orario mensile + buoni spettanti per giorno, un file per lavoratore.
Generato da `create_worker_excel()` (analisi_tivoli.py:853) — colonna delta (maturati-erogati) con fill colore se mismatch (`MISS_FILL`).
← [[Calcolo Buoni]] → [[ZIP Definitivo]]
