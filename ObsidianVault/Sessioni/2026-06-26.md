---
tags: [sessione, pdf, python, corsi, flutter]
---
# Sessione 2026-06-26

## PDF Magic Tool — rework v3.0

Progetto vecchio (`C:\Users\Gianmarco\Python\pdfconverter.py`) riscritto da zero come v3.

**Prima (v1/v2)**:
- 1 solo tab, nessun threading (UI si bloccava), solo conversione PDF, nessuna gestione errori visibile

**Dopo (v3.0)**:
- 4 tab: Converti, Gestisci PDF, PDF/A, Immagini
- Threading su tutte le operazioni (progress bar animata)
- File list con drag & drop + pulsante Sfoglia
- Nuove funzioni: Comprimi, Dividi/Estrai, Ruota, Proteggi/Sblocca password
- **Convertitore PDF/A** (1a/1b/2a/2b/3a/3b): usa Ghostscript se nel PATH, altrimenti PyMuPDF+XMP
- PDF → Immagini (PNG/JPEG/WebP, DPI configurabile)
- Immagini → PDF (assembla più file in un PDF)

**Build**:
- PyInstaller con spec esistente (`PDF_Magic_Tool_Pro.spec` — entry: `pdfconverter.py`)
- Installer Inno Setup: `Setup_PDF_Magicpro.iss`, ISCC in `AppData\Local\Programs\Inno Setup 6\`
- Password installer: `osare199`

**PDF/A standalone (senza Ghostscript)**: implementato in `_pdfa_fitz` con OutputIntent sRGB (profilo ICC estratto da `fitz.Colorspace(CS_RGB).icc_profile()`) + XMP metadata completi. Funziona senza dipendenze esterne.

**Icona**: corretta da singola risoluzione (242x256, header corrotto) a multi-risoluzione ICO (16/32/48/64/128/256px via PIL LANCZOS).

**Build finale (v3.0 funzionante)**:
- PyInstaller: `C:\Users\Gianmarco\Python\dist\PDF_Magic_Tool_Pro\`
- Installer: `C:\Users\Gianmarco\Python\Output\pdfmagictoolpro.exe`
- Installato in: `C:\Program Files\PDF Magic Tool Pro\`
- Icona desktop: `C:\Users\Public\Desktop\PDF Magic Tool Pro.lnk`

**Bug risolti (sessione continua 2026-06-27)**:
- `darkdetect` non bundled → fix: xcopy manuale non basta, bisogna fare rebuild PyInstaller dopo `pip install customtkinter` (così darkdetect viene incluso nell'archivio PKG)
- Icona 7KB era troppo piccola → utente vuole quella originale (hacker con PDF sullo sfondo) → trovata in `C:\Users\Gianmarco\Python\icon.ico` (242x256) → convertita in multi-risoluzione ICO 135KB (7 risoluzioni native)
- `_pdfa_fitz` rotto: usava `doc.add_stream()` (rimossa in PyMuPDF 1.27) + profilo ICC placeholder invalido → fix: `get_new_xref + update_object + update_stream` + profilo ICC reale da `C:\Windows\System32\spool\drivers\color\sRGB Color Space Profile.icm` (3144 bytes) embedded in `_SRGB_ICC_B64` (zlib+base64). Testato: OutputIntents in catalog OK, XMP pdfaid OK.
- App ora si avvia correttamente; PDF/A funziona senza Ghostscript

Nota hub: [[PDF Magic Tool]]

---

## ExamCorrector — UI redesign + Timer esame

Progetto: `C:\Users\Gianmarco\exam_corrector\` (Python/tkinter)

### Miglioramenti UI (prima parte)
- Header doppia riga con subtitle aggiornabile
- Griglia risposte Tab 1: badge numero navy + bottoni toggle A/B/C (`indicatoron=False`)
- KPI cards colorate in Tab 3 (5 card: Studenti/Media/Mediana/Dev.std/Idonei%)
- student_form.py riscritta: ttk, header navy, progress counter live, stile coerente
- Bug fix: chiave JSON `student_name` vs `name` — grader ora accetta entrambe

### Timer esame (seconda parte)
Spinbox "Durata (min)" in Tab 1 params_card (0 = no timer). Salvato in `exam_info.json` e nei config JSON.

**student_form.py** — flusso 2 fasi:
1. Inserisci nome → "Avvia Esame" (verde, in bottom_bar)
2. Timer nel header: countdown MM:SS, giallo <5min, rosso <1min, auto-save a zero

**HTML form** — stessa logica con `setInterval` JS, fasi CSS `.phase.active`.

**Nota tecnica**: `HTML_TEMPLATE` migrato da `str.format()` a `.replace("__PLACEHOLDER__", ...)` — elimina l'escaping `{{}}` in JS/CSS.

### QBGenerator integration (idee discusse)
- Multi-variante: caricare N marksheet (c / c_9 / c_16), studente sceglie la propria → utile per anticopying
- Watch folder: auto-import nuovi PDF QBGenerator
- Auto-detect variante dal nome file (`c_N`)

### KPI cross-sessione (idea discussa)
SQLite locale che accumula risultati per sessione → trend graph Tab 3

**Commit timer**: `4842ad4`

### Bug fix (2026-06-27) — Avvia Esame non reagiva al click (student_form.py — risolto ma irrilevante)
- `ttk.Button` con stile custom nel tema `clam` su Windows può non registrare il click
- Fix: sostituito con `tk.Button` (stile CSS-like inline, più affidabile)
- Aggiunto `root.report_callback_exception` → eccezioni nei callback ora mostrano messagebox invece di sparire silenziosamente
- Aggiunto `root.update_idletasks()` dopo il pack-swap fase1→fase2
- Commit `9235f5e`
- **NOTA**: student_form.py non è usato dagli studenti — aprono direttamente l'HTML generato

### Bug fix (2026-06-27) — Avvia Esame non funzionava nell'HTML (fix reale)
- Root cause: bottone `type="submit"` (default) + nessun `addEventListener` + JS usava `innerHTML +=` su div nascosto al caricamento
- Fix: `type="button"` esplicito, rimosso `onclick` inline, aggiunto `window.addEventListener('load', ...)` con `addEventListener` su btn-avvia/btn-salva
- Spostata costruzione domande in `buildQuestions()` chiamata dentro `startExam()` (non a page load)
- Riscritto JS con `var` (compat. browser), IIFE per closures nei loop, `body.appendChild/removeChild` per il link download
- Aggiunto `window.onerror` per debug visibile
- Fix `/` in exam_name: aggiunta `_safe_fn()` (strip `[/\\:*?"<>|]`) applicata ai 7 `initialfile=` nei file dialog
- `.gitignore` creato (`dist/`, `build/`) — l'EXE era 302MB, troppo grande per GitHub
- Bug aggiuntivo trovato: `'Avvia prima l'esame!'` → apostrofo non escapato → SyntaxError JS globale → nessun listener funzionava. Fix: doppi apici. Commit `c7a144f`
- EXE locale: `C:\Users\Gianmarco\exam_corrector\dist\ExamCorrector.exe`
- IMPORTANTE: dopo ogni rebuild, generare un NUOVO html dall'EXE — gli html vecchi non ricevono le fix automaticamente
- Commit `718f01f` (main.py) + `1c75a8f` (.gitignore)

### Feature (2026-06-30) — Durata default 75s/domanda
- `_on_nq_changed`: trace su `_nq_var`, aggiorna `_duration_var = round(N × 75 / 60)` ad ogni modifica N
- Default iniziale: 13 min (10 domande × 75s)
- Spinbox durata: increment 5→1 per precisione
- EXE rebuild: 19:27 del 30/06/2026
- Commit `2c60b69`

---

## Corsi — Task #77: filtro task senza modulo + progressione ore per task

### Parte A — `lessons_log_tab.dart` (completata in sessione precedente)
Filtro "Task ID" nello storico lezioni del direttore ora funziona senza dover prima selezionare modulo/sottomodulo. `_tasksForFilter` itera su tutti i moduli se nessuno è selezionato. Nuovo helper `_findTask(taskId)` + `_taskProgress` getter. Badge ore nella tabella mostra `done/plannedHours h` per task (non più progressione sottomodulo).

### Parte B — `schedule_tab.dart` (commit `d9d8e09`, dopo 2 tentativi)
Nel calendario del direttore, le celle lezione pratica con `task_id` mostrano `P ord/plannedHours h` con ordinale progressivo (1/N alla prima, 2/N alla seconda, ecc.) — stesso comportamento delle ore teoria.

**Come funziona**:
- `taskOrdinals: Map<String, int>` keyed per `"YYYY-MM-DD_slot_taskId"` (chiave composita deterministica)
- `taskPlanMap: Map<int, double>` (ore pianificate per task ID da reference.json)
- `_lessonCell`: se `!isTheory && lesson.taskId != null` → costruisce la stessa chiave composita e recupera l'ordinale

**Bug intermedii risolti**:
1. `2ec5b7c` — usava `doneTask` (conteggio totale per task), mostrava sempre `N/N` (max/max)
2. `4b981bf` — usava `taskOrdinals` keyed per `l.id` (lesson ID), ancora max/max: `_weekLessons` e `_allCourseLessons` chiamano `getAllLessons()` separatamente → istanze diverse → `id` uguale ma il lookup restituiva null → fallback a `rawOrd` (ordinale sottomodulo) che coincideva col totale ore sottomodulo
3. `d9d8e09` — chiave composita `data_slot_taskId`: deterministica, indipendente dall'istanza → fix confermato
