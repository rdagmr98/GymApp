# GitHub Pages
`rdagmr98.github.io/GymApp/` — Pages source = branch **`main`**, path `/` (verificato via `gh api repos/rdagmr98/GymApp/pages`, 19 giu 2026).
NON `gh-pages` (quel branch esiste ancora sul remote ma è orfano/inutilizzato — fonte di confusione passata, vedi STATO SESSIONE in [[Gym App]]). Build copiati direttamente in root di `main` e pushati.
← [[Web Build]]
