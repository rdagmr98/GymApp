# Sessione 2026-06-27 — Gym App ecosystem

## Progetti toccati
- GymApp trainer (`C:\Users\Gianmarco`, repo `rdagmr98/GymApp`)
- app_cliente / gymapplogbook (`C:\Users\Gianmarco\fix-ads`, repo `rdagmr98/gymapplogbook`)
- app_pt (`C:\Users\Gianmarco\app\miadiariogym\app_pt`)

---

## Modifiche fatte

### GymApp + app_cliente — `_buildRoutinePage()` a altezza variabile
Sostituito ListView fisso con `LayoutBuilder` + `SingleChildScrollView`. Le card dei giorni ora riempiono tutto lo spazio disponibile dividendo `constraints.maxHeight` per il numero di giorni, senza clamp superiore (rimosso il 220dp cap che lasciava spazio nero con pochi giorni). GymApp aggiunge `OutlinedButton` "MODIFICA SCHEDA" in fondo. `_buildRoutineCard()` Stack: aggiunto `alignment: Alignment.centerLeft`.

### GymApp + app_cliente — niente limite esercizi in "Allenati"
Rimosso il `count >= 4` limit in `_buildExPreviewList()` e il testo `+N altri` al sito chiamante. Ora tutti gli esercizi del giorno sono visibili nella preview.

### GymApp — storyCard streak senza altezza fissa
Rimosso `height: 600` dal Container in `_buildStoryCard()`. La card si ridimensiona come app_cliente (non aveva questo problema). Sostituito `const Spacer()` + hashtag Text con `const SizedBox(height: 20)`.

### app_pt — auto-switch a tab PROGRESSI dopo import
Convertito `DefaultTabController` → `TabController` esplicito (campo `_tabController`, `initState`/`dispose`). Aggiunto `_tabController.animateTo(1)` nei tre punti di import: `_handleIncomingProgressFile`, `_importaFileProgressi`, `_importLogManuale`.

---

## Versioni e release
- **GymApp `v1.0.2+24`**: APK arm64/armeabi + AAB in `Documents\releases\gym_app\`, GitHub Release
- **app_cliente**: APK arm64/armeabi aggiornati in `Documents\releases\app_cliente\`, GitHub Release
- **app_pt**: APK arm64 in `Documents\releases\` (prima sessione con APK in release)

---

## Deploy web

### GymApp web — bug deploy corretto
**ERRORE SESSIONE**: ho deployato su `gh-pages` branch invece che su `main` root.  
**Fix**: copiato `build/web/*` nella root di `C:\Users\Gianmarco`, commit `b73673a` su `main` → Pages ha ricostruito (verificato `"status":"built"`).  
Regola confermata: Pages source = `main` root (NON `gh-pages` che è orfano). Build command: `flutter build web --release --base-href "/GymApp/"`. File tracciati in root: `index.html`, `main.dart.js`, `flutter_bootstrap.js`, `flutter_service_worker.js`, `flutter.js`, `version.json`, `manifest.json`, `favicon.png`, `canvaskit/`, `icons/`, `assets/` (compilati web).

### gymapplogbook web
Deploy automatico via GitHub Actions (`web-deploy.yml`). Aggiornato automaticamente al push del commit `e867af4` — verificato `"status":"built"` alle 12:00.

---

---

## Sessione corsi (sessione 23) — sistema notifiche + aggiornamenti pending

### Cosa è stato fatto
Implementazione completa delle 3 feature richieste in sessione precedente (compattata):

**Feature 1 — Aggiornamenti istruttore in attesa di validazione**
- `InstructorUpdate.status` (`pending`/`approved`/`rejected`), backcompat `'approved'` su record esistenti
- `GradeService.addPendingUpdate()` — salva con `status: 'pending'`
- `GradeService.approveUpdate()`, `getPendingUpdates()`
- Filtri pending in currency: `getManualTeachingHoursRollingYear`, `getProfessionalUpdateHoursLast2Years`, `_teachingByYear` skip pending
- `MyHoursScreen`: pulsante "Aggiungi" → dialog (tipo/desc/ore/data) → `addPendingUpdate` + notifica admin; chip "In attesa" sui record pending
- `CurrencyTab`: sezione "Aggiornamenti in attesa" con pulsanti Approva/Rifiuta

**Feature 2/3 — Sistema notifiche in-app**
- `AppNotification` model (`notification_models.dart`)
- `NotificationService` (CRUD su `notifications.json`, deduplicazione, sincronizzazione browser)
- `WebNotificationService` (dart:html, Web Notification API, stesso pattern AVES)
- `NotificationPanelWidget` (bottom sheet, swipe-to-delete, badge)
- Campana + badge in tutti e 4 i ruoli (`InstructorShell`, `AttendeeShell`, `DirectorShell`, `MasterShell`)
- `AuthProvider.unreadCount` inizializzato al login, decrementato alla lettura
- Trigger notifiche:
  - `ScheduleTab._addLesson` → `notifyLessonScheduled` (frequentatori + istruttore)
  - `ScheduleTab._editLessonInstructor` → `notifyLessonChanged` (nuovo istruttore)
  - `TodayScreen._confirmLesson` → `notifyLessonValidated` (direttori del corso)
  - `TodayScreen._generateDayBeforeReminders` → `notifyLessonReminder` (istruttore, dedupeUnread)
  - `MyHoursScreen._showAddUpdateDialog` → `notifyUpdateSubmitted` (tutti gli admin)
  - `CurrencyTab._PendingUpdatesSection` → `notifyUpdateApproved` (istruttore)

### Commit
- `corsi` app: `69621b7` (16 file, 1119 ins)
- `corsi-data`: `458f0ee` (notifications.json inizializzato `[]`)

### Build
- `flutter build web --release --base-href "/corsi/"` → OK, 75.9s
- GitHub Actions deploya automaticamente

### GymApp — info panel e native ad stessa altezza (workout screen)
Nella schermata workout, il pannello info (ultima serie, chip suggerimento, note coach, note personali) e il native ad inline stavano in una Row con `crossAxisAlignment.start` — altezze diverse. Fix: `IntrinsicHeight` + `CrossAxisAlignment.stretch` + `mainAxisSize.max` sul Column dell'info panel + `Spacer()` prima del TextField note (per ancorarlo in basso). Commit `d4683f5`.

## Note operative
- Il banner AdMob di GymApp non si vede sull'emulatore (normale) — serve device fisico con test device ID registrato
- GIF compatibilità: app_pt esporta `gifFilename` (slug senza estensione), `exerciseAnimationAssetPath(slug)` aggiunge `.webp` → stessa gif in entrambe le app
