---
tags: [sessione, caae, materiale-didattico]
---
# Sessione 2026-06-30

**Progetti toccati**: Materiale Didattico CAAE (engine PDF AER(EP).P-147) — vedi [[materiale_didattico_caae]]

Richiesta utente: 1) i titoli non devono restare orfani in fondo pagina col corpo nella successiva ("meglio spezzare il testo che dividere titolo e corpo"); 2) leggere la chat Gemini aperta in Edge e valutare se usarne le immagini (es. uno spettro della luce "più bello", che l'utente sospettava Gemini prendesse da Getty/Shutterstock).

---

## Fix paginazione — titoli mai orfani

`_engine/engine.py`:
- `keepWithNext=True` sugli stili `h1` e `h2` → reportlab 4.5.0 lega ogni titolo all'inizio del flowable successivo (il corpo segue il titolo, mai da solo a fondo pagina).
- `data_table()` ora ritorna una **lista** di flowable e accetta `keep=` : `True` avvolge in `KeepTogether` (tabelle che stanno in una pagina), `False` lascia spezzare la tabella su più pagine. Aggiornati 3 chiamanti (`render_body`, revision record, glossario) da `F.append` a `F.extend`.
- **Bug risolto**: il glossario (32 righe) avvolto in `KeepTogether` era più alto di una pagina → non potendo stare insieme spingeva il titolo "Glossario" da solo sulla pagina dopo. Fix: glossario con `keep=False` (si spezza), così `keepWithNext` lega il titolo alle prime righe.
- Verifica: script fitz su tutti gli 11 PDF → **0 titoli orfani**. A vista pag. 7 di 53.1 (titolo "Le onde elettromagnetiche" col suo corpo).

## Spettro EM rifatto (figura editoriale)

`_engine/figures.py` — riscritto `fig_em_spectrum()` + helper `_wl_to_rgb` (Bruton, nm→RGB), `_rainbow`, `_vgrad`:
- 5 bande con gradiente verticale (radio indaco, microonde blu, IR rosso/arancio, **visibile = arcobaleno spettrale reale**, UV viola), nomi+sottotitoli sopra, intervalli λ sotto, frecce f/λ, **banda visibile esplosa** con scala in nm 700→400 (rosso→violetto), relazione c = λ × f. Tutto matplotlib, zero watermark. Rigenerata cache e ricostruiti tutti i PDF (11).

## Immagini Gemini — verifica fonte (decisivo)

Edge non era in debug; rilanciato `aiTool/avvia_edge_debug.bat` (utente ha scelto questa via). La chat aperta era stata chiusa dal riavvio; aperta da cronologia una chat affine e letti gli `<img>` via Playwright/CDP. **Esito**: le immagini che Gemini "restituisce" hanno `src = https://encrypted-tbn*.gstatic.com/licensed-image?...` (alt es. "Waves Diffraction", "Piezoelectric Accelerometer") → sono **thumbnail di stock con licenza** (Getty/Shutterstock/Alamy), 0×0, watermarkate e a pagamento. Conferma la policy [[caae-images-workflow]]: **inutilizzabili in un documento MTO certificato**. Decisione confermata: schemi generati da me con matplotlib; foto reali solo da fonti libere (Wikimedia/DVIDS). Gemini resta utile solo per testo/scouting, non per scaricare immagini.

## Stato

50.1 / 51.1 / 53.1 renderizzati e verificati su tutti i corsi (11 PDF, paginazione corretta). Prossimi: 53.2 (Registrazione immagini, Sys 97), 53.3 (Guerra elettronica, Sys 99), 54.1, 55.1.
