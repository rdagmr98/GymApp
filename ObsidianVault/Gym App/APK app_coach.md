# APK app_coach
`flutter build apk --release --target android-arm64` → `GymCoach-arm64.apk`
**Non verificato**: nessun checkout locale di `gymapp-coach` trovato sul disco, hub la segna "sviluppo, nessuna modifica recente" — comando/output per analogia con gym_app, non confermato da una build reale.
← [[app_coach]] → [[releases]]
